server:
  port: 8081
spring:
  application:
    name: bank-core
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true     # /actuator/health/liveness + /readiness for k8s probes
